import { Box, Container, Stack, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from "@mui/material";
import React from "react";

const Alltable = ({placementsData}) => {
  return (
    <>
      <Container>
      
      </Container>
    </>
  );
};

export default Alltable;